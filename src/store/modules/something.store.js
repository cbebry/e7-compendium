const state = {
    //
}

const getters = {
    //
}

// Actions can be asynchronous
const actions = {
    //
}

// Mutations must always be synchronous
const mutations = {
    //
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
